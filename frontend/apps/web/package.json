{
  "name": "web",
  "scripts": {
    "dev": "next dev --turbopack --port 4000",
    "build": "next build",
    "start": "next start --port 4000",
    "lint": "next lint"
  },
  "dependencies": {
    "@pitch-in/shared": "workspace:*"
  }
}
